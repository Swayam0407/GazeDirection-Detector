/**
 * Color palette for predicted classes.
 */
export declare const palette: string[];
/**
 * RGB color object
 */
export type RGB = {
    r: number;
    g: number;
    b: number;
};
/**
 * convert a hex color to RGB
 */
export declare function hexToRgb(hex: string | null | undefined): RGB;
/**
 * Check if a color is dark color or not.
 *
 * For example:
 *
 * ```javascript
 * const textColor = isDark(someColor) ? 'white' : 'black';
 * ```
 */
export declare function isDark(rgb: string | null | undefined | RGB): boolean;
//# sourceMappingURL=colorUtils.d.ts.map